#!/usr/bin/env bash

prevent_sudo_or_root
set -e

# ─── Colors & Logging ─────────────────────────────
log()   { echo -e "\033[1;32m[INFO]\033[0m $(pretty_path "$1")"; }
warn()  { echo -e "\033[1;33m[WARN]\033[0m $(pretty_path "$1")"; }
error() { echo -e "\033[1;31m[ERROR]\033[0m $(pretty_path "$1")"; }

echo_cmd() { echo -e "\033[1;32m───\033[0m $(pretty_path "$1")"; }
log_cmd()  { echo -e "\033[1;32m───>\033[0m $(pretty_path "$1")"; }
run_cmd() {
    # Build a pretty display of the command, replacing $HOME with ~
    local display_cmd=()
    for arg in "$@"; do
        display_cmd+=("$(pretty_path "$arg")")
    done

    echo -e "\033[1;34m>>> ${display_cmd[*]}\033[0m"
    "$@"
}

header()   { echo -e "\n\033[1;35m─── $1 ───\033[0m\n"; }

pretty_path() {
    local path="$1"
    # Absolute path starting with $HOME → replace with ~
    if [[ "$path" == "$HOME"* ]]; then
        echo "~${path#$HOME}"
    else
        echo "$path"
    fi
}


# ─── Continue with your existing vars ──────────
source ./scripts/lib/environment-variables.sh
source ./scripts/lib/functions.sh
source ./scripts/lib/dist-determine.sh

# ─── Detect Shell & Distro ──────────────────────
USER_SHELL=$(basename "$SHELL")
#DISTRO="$OS_GROUP_ID"
#echo $DISTRO
#echo $OS_GROUP_ID
#print_os_group_id

export OS_GROUP_ID
export OS_DISTRO_ID
export OS_DISTRO_ID_LIKE
export DISTRO
export PKG_DIR



# ─── Menu (Manual Mode) ─────────────────────────
show_menu() {
    header "Nixie's Multi-Distro Bootstrap"
    log_cmd "Detected distro: $OS_GROUP_ID"
    log_cmd "Detected shell: $USER_SHELL"

    # Menu titles
    options=(
        "Update System Packages"
        "Install Packages"
        "Symlink Configs"
        "Setup Shell"
        "Install Flatpaks"
        "Exit"
    )

    # Matching scripts (relative to SCRIPTS_DIR)
    scripts=(
        "update_system.sh"
        "install_pkgs.sh"
        "symlink.sh"
        "setup_shell.sh"
        "install_flatpak.sh"
    )

    while true; do
        for i in "${!options[@]}"; do
            echo -e "\033[1;32m$((i+1)).\033[0m ${options[$i]}"
        done

        read -rp $'\nChoose what to run (or q to quit): ' choice
        case "$choice" in
            1) v "$SCRIPTS_DIR/${scripts[0]}" ;;
            2) v "$SCRIPTS_DIR/${scripts[1]}" ;;
            3) v "$SCRIPTS_DIR/${scripts[2]}" ;;
            4) v "$SCRIPTS_DIR/${scripts[3]}" ;;
            5) v "$SCRIPTS_DIR/${scripts[4]}" ;;
            6|q|Q|e) echo_cmd "Goodbye!"; break ;;
            *) warn "Invalid choice." ;;
        esac
        echo
    done
}

x show_menu

pkgx_intro() {
    echo ""
    echo ""
    echo -e "\033[1;32m─────>\033[0m pkgx is a cross-distro package frontend."
    echo -e "\033[1;32m─────>\033[0m Features:"
    echo -e "\033[1;32m─────>\033[0m   • Unified commands across distros"
    echo -e "\033[1;32m─────>\033[0m   • Optional, non-invasive"
    echo -e "\033[1;32m─────>\033[0m   • Installs to user space by default"
    echo -e "\033[1;32m─────>\033[0m   • Supports pacman, pkg, and apt."
    echo ""
}

if [[ -f "$SCRIPTS_DIR/pkgx.sh" ]]; then 
    pkgx_intro
    source $SCRIPTS_DIR/pkgx.sh
    showfun install_pkgx
    echo ""
    z install_pkgx
fi

#if [[ $OS_GROUP_ID == arch ]]; then
#    source $SCRIPTS_DIR/hl-dots.sh
#    showfun install_hl_dots
#    v install_hl_dots
#fi
# ─── Auto Mode Execution ────────────────────────
# if $AUTO_MODE; then
# 
#     header "Nixie's Multi-Distro Bootstrap"
#     log_cmd "Detected distro: $DISTRO"
#     log_cmd "Detected shell: $USER_SHELL"
# 
#     # System Update
#     if ! $SKIP_UPDATE; then
#         echo_cmd "Running system update..."
#         x "$SCRIPTS_DIR/update_system.sh"
#     else
#         echo_cmd "Skipping system update as requested."
#     fi
# 
#     # Sequential auto-run scripts
#     for script in \
#         "install_pkgs.sh" \
#         "symlink.sh" \
#         "setup_shell.sh" \
#         "install_flatpak.sh"
#     do
#         if [[ -f "$SCRIPTS_DIR/$script" ]]; then
#             v "$SCRIPTS_DIR/$script"
#         else
#             warn "Missing script: $SCRIPTS_DIR/$script"
#         fi
#     done
# 
#     echo_cmd "Bootstrap complete. Enjoy your setup 🎉"
# 
# else
#     v show_menu
# fi

